from __future__ import annotations

from typing import Any


def publish_activity_payload(*, payload: dict[str, Any]) -> None:
    """
    Publish an activity payload to the realtime layer.

    Replace this function when Redis, SSE, or Channels is wired in.
    """
    return None