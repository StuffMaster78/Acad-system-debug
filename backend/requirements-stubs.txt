# Type stubs for local IDE / Pyright usage only.
# These are NOT installed in Docker. The app runs entirely inside containers.
#
# To enable full type checking in your IDE:
#   python3 -m venv .venv
#   source .venv/bin/activate   # or .venv\Scripts\activate on Windows
#   pip install -r requirements-stubs.txt
#
# Then update pyrightconfig.json:
#   "venvPath": ".",
#   "venv": ".venv"
#
# The .venv is for IDE tooling only — never use it to run the application.

django-stubs[compatible-mypy]==5.1.3
djangorestframework-stubs==3.15.2
stripe==11.4.1
celery-types==0.22.0
