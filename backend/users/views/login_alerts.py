"""
Login Alert Preferences ViewSet — stub.

LoginAlertPreference model was never migrated (no DB table). This view is
not wired in any URL config. Keeping the file so the module path stays
valid; implement when the model is properly introduced.
"""
